<?php
require_once __DIR__ . '/config/database.php'; require_once __DIR__ . '/config/session.php'; if(current_user()) redirect('dashboard.php'); $error='';
if($_SERVER['REQUEST_METHOD']==='POST'){if(!verify_csrf($_POST['csrf']??null))$error='Your form session expired.';else{$stmt=db()->prepare('SELECT id,full_name,password_hash,role FROM users WHERE email=? LIMIT 1');$stmt->execute([strtolower(trim($_POST['email']??''))]);$user=$stmt->fetch();if($user&&password_verify($_POST['password']??'',$user['password_hash'])){session_regenerate_id(true);$_SESSION['user']=['id'=>(int)$user['id'],'full_name'=>$user['full_name'],'role'=>$user['role']];redirect($user['role']==='admin'?'admin/index.php':'dashboard.php');}$error='Email or password is incorrect.';}}
$title='Log in'; require __DIR__.'/includes/header.php'; ?><div class="auth-wrap"><div class="auth-card"><div class="eyebrow mb-2">Welcome back</div><h1 class="h2 mb-4">Pick up where you left off.</h1><?php if($error):?><div class="alert alert-danger"><?=e($error)?></div><?php endif;?><form method="post"><input type="hidden" name="csrf" value="<?=e(csrf_token())?>"><label class="form-label">Email</label><input class="form-control mb-3" name="email" type="email" required><label class="form-label">Password</label><input class="form-control mb-4" name="password" type="password" required><button class="btn btn-dark w-100 py-3 rounded-pill">Log in <i class="bi bi-arrow-right"></i></button></form><p class="text-muted mt-4 mb-0">New here? <a href="<?=APP_URL?>/register.php">Create a profile</a></p></div></div><?php require __DIR__.'/includes/footer.php'; ?>
